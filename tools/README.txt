FakeCarbon -
    This is a simple tool to emulate what the part of Graphite that actually gets the stats does. It has flags to
    only display packets with errors and so on. It only captures stuff to logs at this point.